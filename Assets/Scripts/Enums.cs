//이곳에 enums 들 정의
public enum WeaponType
{
    None,
}

public enum PlayerState
{
    Idle,
    Move,
    Dodge,
    Parrying,
    Die,
}

public enum BossState
{
    Idle,
    Move,
    Pattern,
    Die,
}